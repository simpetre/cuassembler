# Python bytecode
__pycache__/
*.pyc
*.pyo

# Corpus build cache — regenerated by Tools/training_corpus/build_feeder.sh
# (compiled probe cubins + intermediate .sass dumps; the *sources* under
# 0x_*/ and the solved repos in CuAsm/InsAsmRepos/ are NOT ignored).
Tools/training_corpus/_build_*/

# OS / editor cruft
.DS_Store
*.swp
*~
